const SectionTitle = ({ title, subtitle }) => {
    return (
        <div>
            <h1 className="text-3xl font-bold text-center my-10">{title}</h1>

        </div>
    );
};

export default SectionTitle;